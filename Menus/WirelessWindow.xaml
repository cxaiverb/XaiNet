<Window x:Class="XaiNet2.WirelessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wi-Fi" Width="350" Height="400"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        Deactivated="Window_Deactivated">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="10" Background="#AA222222" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid VerticalAlignment="Top" HorizontalAlignment="Left" ShowGridLines="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        
                        <Button Name="HomeButton" Click="HomeButton_Click"
                                Width="32" Height="32"
                                Background="Transparent" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Grid.Column="0"
                                Grid.Row="0"/>
                        <Button Name="RefreshButton" Click="RefreshWiFi_Click"
                                Width="32" Height="32"
                                Background="Transparent" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Grid.Column="1"
                                Grid.Row="0"/>

                        <TextBlock Text="Wi-Fi Networks" Foreground="White" 
                                   FontSize="16"
                                   Grid.Column="2"
                                   Grid.Row="0"
                                   VerticalAlignment="Center"/>
                    </Grid>
                    <StackPanel Name="AvailableNetworks">
                        <StackPanel Name="AvailableNetworksPanel" Margin="10">
                            <TextBlock Text="No Wi-Fi adapter detected." Foreground="LightGray" Name="NoWiFiTextBlock"/>
                        </StackPanel>
                        <ItemsControl Name="WiFiNetworkList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid ShowGridLines="True" VerticalAlignment="Top" HorizontalAlignment="Left">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{Binding SSID}" Foreground="White" FontSize="14" Margin="5" Grid.Column="2" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding SignalStrength}" Foreground="LightGray" Margin="5" Grid.Column="1" VerticalAlignment="Center"/>
                                        <Button Content="Connect" Click="ConnectToWiFi_Click"
                                                Tag="{Binding SSID}" 
                                                Background="Gray" Foreground="White" 
                                                HorizontalAlignment="Right"
                                                Width="80" Margin="5,0,0,0" Grid.Column="0"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
    
</Window>
